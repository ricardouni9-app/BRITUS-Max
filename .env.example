# -----------------------------------------------------------------------------
# BRITUS Platform — variáveis de ambiente (exemplo).
# Copie para `.env` e ajuste. NUNCA versione o `.env` real nem credenciais reais.
# -----------------------------------------------------------------------------

# Banco de dados PostgreSQL — valor FICTÍCIO (não usar em produção).
DATABASE_URL="postgres://user:password@localhost:5432/britus_dev"
