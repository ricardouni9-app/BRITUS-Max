{
  "name": "@britus/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "pilot": "node --experimental-strip-types src/index.ts",
    "build": "tsc --build",
    "start": "node dist/index.js",
    "commercial": "node dist/index.js",
    "bootstrap": "node dist/bin/bootstrap.js",
    "typecheck": "tsc --build",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@britus/application": "workspace:*",
    "@britus/contracts": "workspace:*",
    "@britus/db": "workspace:*",
    "@node-rs/argon2": "^2.0.2",
    "fastify": "^5.10.0",
    "uuidv7": "^1.2.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
