# @britus/ui

**Design system** compartilhado (componentes de interface reutilizáveis) da
BRITUS Platform.

Nasce apenas quando houver reuso real entre telas — evita abstrair cedo demais.
A capacidade "componentes de UI" é nomeada pela capacidade, não pela tecnologia.

> **Estado atual:** apenas o espaço estrutural. Nenhum componente foi criado
> (Etapa 5 não implementa telas/componentes). Será populado a partir da Sprint 1.
